# Copy this file to ".env" and fill in your keys (or just use the web UI, which
# writes this file for you). Everything stays local on your machine.

# Claude (only needed if you switch the LLM provider to "claude" in the UI).
# Ollama is the default and needs no key.
ANTHROPIC_API_KEY=

# Stock B-roll auto-fetch (free key from https://www.pexels.com/api/)
PEXELS_API_KEY=

# Background music / SFX (free key from https://pixabay.com/api/docs/)
PIXABAY_API_KEY=
